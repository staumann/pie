<!doctype html>
<html>
    {{> header}}
    <body>
        <div class="wrapper">
            <h2>Overview</h2>
            {{#categories}}
            <div class="card mt-2">
                <div class="card-body">
                    <h5 class="card-title">Category: {{name}}</h5>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">Name: {{name}}</li>
                        <li class="list-group-item">Description: {{description}}</li>
                    </ul>
                    <div class="text-center">
                        <a href="/show/category/details?id={{id}}" class="btn btn-primary">Show</a>
                    </div>
                </div>
            </div>
            {{/categories}}
        </div>
    </body>
</html>